## 2025-05-14 - Accessible Form Controls
**Learning:** Standard React labels without `htmlFor` and `id` are not accessible to screen readers and limit the clickable area for users. Adding these attributes along with `accept` on file inputs significantly improves the initial interaction quality.
**Action:** Always link labels to inputs and restrict file input types to relevant extensions to prevent user errors and improve accessibility.
